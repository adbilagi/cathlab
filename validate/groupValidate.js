/**
 * This file validates the groupSchema
 */ 
const validator = require("validator");
const roles = require("../roles/roles");

module.exports =(curJson)=>{
    // write for each fileds of groupSchema mongoose
}
